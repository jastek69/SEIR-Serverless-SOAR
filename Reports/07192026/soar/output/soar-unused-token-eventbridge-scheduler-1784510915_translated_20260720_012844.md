# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784510915-2026-07-20_01-28-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 01:28:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**評価対象の重要度:情報提供のみを目的とする/低い (運用上のギャップ — アクティブな脅威ではない) **

**理由:**

検出器は EventBridge スケジューラで予定どおりに起動し、エラーなく完了しました。ただし、`records_ated: 0` は、ディテクターが評価すべき**DynamoDB テーブルの `トークントラッキング`**にトークントラッキングレコードが見つからないことを意味します。これは運用上の確認であり、確認済みのセキュリティインシデントではありません。

**証拠から確認されたこと:**
-検出器は正常に実行されました (エラーやクラッシュではなく、検出結果がゼロの構造化された出力が生成されました)。
-DynamoDB テーブルの「トークントラッキング」には、評価時にレコードがまったく含まれていなかったか、ディテクタが使用したクエリ条件に一致するレコードがゼロでした。
-追跡対象として登録されたトークンがないため、未使用のトークンにはフラグが付けられませんでした。

**証拠で確認できないこと:**
-本物の Cognito JWT が発行され、未使用になったこと。セキュリティイベントの説明 (「ユーザー認証に成功し、JWT トークンは発行され、トークンは 15 分以内に使用されなかった」) には、このディテクターが検知するように設計されている「動作クラス」が記述されています。特定のユーザーセッションがこの実行をトリガーしたかどうかは確認しません。
-攻撃者がトークンを収集したこと。提供されたデータには、認証情報の盗難、トークンの再生、またはラテラルムーブメントの証拠はない。

>モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
