# SOAR レポート-未使用トークンマニュアル-1784516682-2026-07-20_03-04-42_UTC

-トリガー:マニュアル
-生成済み:2026-07-20T 03:04:42 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:オペレーターが未使用のトークンのレビューをリクエストした
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用の JWT トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**重要度:情報提供**
**信頼度:高い**

**正当化:**

このイベントで確認された証拠は限定的で明確です。「unused_token_detector」はオペレーターが手動でトリガーし、**ゼロレコード**を調べ、**ゼロレコード**を照合し、**ゼロレコード**を生成し、**ゼロアラート**を生成しました。提供されたデータには、所見、異常な認証イベント、未使用のトークン、侵害の兆候は確認されていません。

仮定の攻撃経路に基づいて重大度を高めることはできません。調査された記録がないこと自体が主要な発見です。これは**検出器のカバレッジギャップ**であり、セキュリティインシデントではありません。重要度を高く設定すると、このイベントには存在しない悪意のあるアクティビティの証拠が確認されなければなりません。

**確認された内容:**
-検出器は正常に実行されました (エラーではなくカウントがゼロの構造化された結果が生成されました)。
-DynamoDB テーブルの「トークントラッキング」は、実行時にディテクターにゼロレコードを返しました。
-追跡メカニズムは**設計上オプトイン**になっています。`mfa_bootstrap.py--track-token` で明示的に登録されたトークンまたは合成マーカーのみがテーブルに表示されます。
-Cognitoが発行した他のクライアントフローからのJWTトークンは、**自動的には登録されない**ため、**この検出器からは見えません**。

> >モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
