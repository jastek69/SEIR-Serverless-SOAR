# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784533715-2026-07-20_07-20_07-48-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 07:48:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**評価対象の重要度:情報提供 (条件付きLOW勧告あり) **

**確認された証拠のみに基づく正当化:**

検出器は EventBridge スケジューラー経由で予定どおりに起動しました。検査されたレコードはゼロで、マッチしたレコードはゼロでした。アラートは発生せず、結果も生成されませんでした。この呼び出しには、悪意のある認証イベント、認証情報が侵害された、またはトークンが悪用されたという確認された証拠はありません。

シナリオの説明 (「ユーザーの認証に成功し、JWT トークンは発行され、トークンは 15 分以内に使用されなかった」) では、このディテクターが検知しようとしている*イベントのクラス*について説明しています。これは、この特定の呼び出しから確認された結果を示すものではありません。JSON ペイロードには、「レコードの確認:0」、「一致:0」、「アラート:0」が明示的に表示されます。

**なぜ重大度を上げないのか:**
説明したシナリオに基づいて重要度を「中」または「高」に割り当てるには、`findings_sample` に一致するレコードが少なくとも 1 つ必要です。何も存在しません。検出器が何も検出しなかったのに、仮想的な攻撃経路から重大度がエスカレートすると、誤った緊急度が生じ、アラートパイプラインに対するアナリストの信頼が損なわれます。

**条件付き LOW 勧告** は攻撃には適用されず、**検出ギャップ** に適用されます。実際の認証イベントが発生した場合でも `records_examined` が 0 であるという事実は、このトークンを示唆しています。

> >モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
