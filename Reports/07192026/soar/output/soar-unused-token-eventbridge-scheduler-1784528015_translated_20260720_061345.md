# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784528015-2026-07-20_06-13-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 06:13:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**評価対象の重要度:情報提供のみを目的とする/低い (運用上のギャップ — アクティブなセキュリティインシデントではない) **

**正当化:**

検出器は予定どおりに起動し、エラーなく完了しました。重要な発見は、攻撃や侵害が確認されたということではありません。つまり、評価時に DynamoDB テーブルの「トークントラッキング」には**レコードがまったく**含まれていなかったという意味で、`records_examted`: 0`でした。一致するトークンはなく、アラートも発生せず、結果も生成されませんでした。

この結果には次の 2 つの解釈が考えられます。

| 解釈 | 証拠の根拠 | セキュリティ上の含意 |
|---|---|---|
| スキャン時にトラッキングテーブルにトークンが登録されませんでした | 確認済み — `records_examted:0` | ディテクタは正常に動作したが評価するものは何もなかった |
| 検出器には登録ギャップがあり、実際のトークンは可視範囲外に存在する | 実装上の事実からも確認できる — `mfa_bootstrap.py--track-token` で明示的に登録されたトークンのみが追跡される | 正規のCognitoセッションは、この検出器から完全に見えなくなる可能性がある |

**確認されている動作上のギャップ** は、ディテクタの適用範囲が明示的なトークン登録によって制限されていることです。`--track-token` ブートストラップフローの外部で発行された Cognito ID トークンはすべて「トークントラッキング」に登録されないため、次のように検出することはできません。

>モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
