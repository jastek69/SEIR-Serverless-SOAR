# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784528615-2026-07-20_06-23-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 06:23:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**重要度:情報提供用 (調査用の運用フラグ付き) **

**確認された証拠のみに基づく正当化:**

検出器は EventBridge スケジューラー経由で予定どおりに起動しました。検査されたレコードはゼロで、マッチしたレコードはゼロでした。アラートは発生せず、結果も生成されませんでした。このテレメトリには、悪意のある認証イベント、侵害された認証情報、またはアクティブな脅威アクターの証拠は確認されていません。

シナリオの説明 (「ユーザーの認証に成功し、JWT トークンは発行され、トークンは 15 分以内に使用されなかった」) では、このディテクタの「意図された検出対象」について説明しています。この特定の呼び出しから検出されたことが確認されたわけではありません。ディテクターは `records_examine: 0` を返しました。つまり、評価の対象となるレコードがまったく見つからなかったということです。これは、レコードを検索して未使用であると判断した結果とはまったく異なります。

**この結果が「高」または「重要」ではない理由:**

ディテクタが何も検出しなかったのに、仮定の攻撃経路（トークン・ハーベスティングやクレデンシャル・スタッフィングなど）に基づいて重大度を上げることは、分析的に不適切です。証拠はそれを裏付けるものではない。ここでの運用上の懸念は、より狭く、より正確である。*なぜ検出器はゼロレコードを調べたのか？* その質問には、提供されている実装上の事実に基づいた、悪意のない既知の回答があります。これについては、セクション 2 で取り上げています。

**

>モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
