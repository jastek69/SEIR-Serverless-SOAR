# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784508215-2026-07-20_00-43-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 00:43:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:0
-アクティブな未使用のレコードが一致しました:0
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用トークン検出器 — ゼロレコードが検証されました

---

## 1。重要度評価

**重要度:情報提供 (確認済みのセキュリティインシデントではない) **

**理由:**

検出器は予定どおりに起動し、エラーなく完了しました。`records_examine: 0`、`matched: 0`、`alerted: 0`、`findings_total: 0` の各フィールドを合わせると、DynamoDB テーブルの「トークントラッキング」にはクエリ時に**レコードがゼロ**が含まれていたことがわかります。レコードが存在してフィルタリングされたということではありません。これは重要な違いです。

この事件では、脅威の証拠は確認されていません。セキュリティイベントの説明 (「ユーザー認証成功 → JWT トークンの発行 → トークンが 15 分以内に使用されなかった」) には、ディテクターがキャッチするように設計されている「意図された検出シナリオ」が記述されていますが、結果がゼロということは、ディテクターが**追跡対象のトークンをまったく見つけられなかった**ことを意味します。未使用のトークンは見つからず、警告もされませんでした。

**このイベントで確認された証拠:**
-イベントブリッジスケジューラーは「unused_token_detector」ラムダを正常に呼び出しました。
-ラムダは実行を完了し、結果を報告しました。
-DynamoDB テーブルの「トークントラッキング」から、確認すべきレコードがゼロが返されました。
-アラートは生成されませんでした。

**実装ギャップ (確認済み、仮説ではない): **
-「implementation_facts」には、「mfa 経由で登録されたトークンのみ」と明記されています

>モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
