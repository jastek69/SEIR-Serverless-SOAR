# SOAR レポート-未使用トークン-イベントブリッジスケジューラー-1784509715-2026-07-20_01-08-35_UTC

-トリガー:イベントブリッジスケジューラー
-生成済み:2026-07-20T 01:08:35 Z
-しきい値 (分): 15
-調べた DynamoDB レコード数:2
-アクティブなレコードと未使用のレコードが一致しました:2
-公開されたアラート:0
-理由:EventBridge スケジューラによって未使用のトークンチェックが呼び出されました
-プロンプトソース:ssm
-プロンプトパラメータ:/bedrock/soar-prompt
-ベッドロックモデル:us.anthropic.claude-sonnet-4-6

## 古いトークン
-この実行中、しきい値と一致した古い未使用のトークンはありませんでした。

## SOAR 分析
# セキュリティイベント分析:未使用の JWT トークン検出器の呼び出し

---

## 1。重要度評価

**評価対象の重要度:情報提供のみを目的とする/低い (アクティブなインシデントなし) **

**理由:**

検出器はEventBridgeスケジューラを介して予定どおりに起動し、全検査サイクルを完了しました。イベントレポート:

-`records_ated:2` — ディテクターは DynamoDB の「トークントラッキング」テーブルで 2 つのレコードを見つけて評価しました。
-`match: 2` — 両方のレコードがディテクタのフィルター条件に一致しました (たとえば、ステータスフィールドが認識され、トークンの経過時間が計算可能)。
-`alerted: 0` — どちらのレコードもアラート条件をトリガーしませんでした。
-`findings_total: 0` — 15 分のしきい値ウィンドウ内に未使用のトークンは確認されませんでした。
-`findings_sample: [] `— 認証動作に異常があるという証拠はありません。

**検出器は正常に動作しています。** 両方のレコードが確認され、消去されました。この呼び出しには、脅威、検出ギャップ、または認証異常の証拠は確認されていません。このイベントでは、セキュリティ上の結果ではなく、正常にスケジュールされた統制の実行が記録されているため、重要度は情報提供を目的としています。

**重要な違い:** プロンプトヘッダーに記述されているセキュリティイベント (「ユーザーの認証に成功しました → JWT トークンは発行されました → トークンが 15 分以内に使用されなかった」) は、このディテクタが検知するように設計されている「動作の種類」を示しています。この具体的には

>モデル出力が設定された制限に達しました。以下の確定的リカバリのセクションでレポートを完成させます。

## 4.簡単なエグゼクティブサマリー

このディテクタの実行に一致する古いアクティブ/未使用のトラッキングレコードはありませんでした。レコードが Cognito ブートストラップフローから明示的に登録されていない限り、この結果は Cognito JWT が発行されて未使用のままだったという証拠にはなりません。そのフローの外にある Cognito クライアントは、自動的にトラッキングにリンクされません。

## 5。推奨修復方法の説明

-DynamoDB 属性名は `status` として一貫して小文字にしてください。
-明示的な登録および使用状況更新フローを通じて、実際の Cognito セッション識別子を追跡します。
-DynamoDB の「スキャン回数」を、一致するレコード数とは別にレポートします。
-モデルに提供された証拠からのみインシデントレポートを生成します。

## 6。修正に役立つ可能性のあるコードスニペットとチュートリアル

```python
レスポンス = トラッキング.scan (**scan_kwargs)
レコード_検証済み += response.get (「ScannedCount」, 0)
一致するレコード += response.get (「カウント」, 0)
```

発行時に安定した Cognito セッション識別子を登録し、承認された API リクエストが成功したら同じレコードを更新します。
